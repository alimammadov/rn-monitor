export const monitorConfig = {
  slowNetworkThresholdMs: 1000,
  enableConsoleLogs: true,

  jsLagCheckIntervalMs: 100,
  jsLagThresholdMs: 200,

  fpsSampleIntervalMs: 1000,
  lowFpsThreshold: 45,
};